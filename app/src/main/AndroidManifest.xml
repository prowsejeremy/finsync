<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jpd.finsync">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Storage: legacy API <= 28 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- Android 13+ read media -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!--
        MANAGE_EXTERNAL_STORAGE: required on Android 11+ to write to arbitrary
        public directories (e.g. /sdcard/Music/...).
        If not granted the app falls back to its own external files dir, which
        needs no permission and is always writable.
        Note: apps using this must declare it in Play Store listing.
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />


    <application
        android:name=".Finsync"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Finsync"
        android:supportsRtl="true"
        android:theme="@style/Theme.Finsync"
        android:requestLegacyExternalStorage="true">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Permissions onboarding — the true entry point on fresh install -->
        <activity
            android:name=".ui.PermissionsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Finsync.Main" />

        <activity
            android:name=".ui.LoginActivity"
            android:exported="false"
            android:label="@string/title_login"
            android:theme="@style/Theme.Finsync.Login"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.LibraryActivity"
            android:exported="false"
            android:label="@string/title_library"
            android:theme="@style/Theme.Finsync.Main" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/title_settings"
            android:theme="@style/Theme.Finsync.Main" />

        <service
            android:name=".service.SyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>


    </application>

</manifest>