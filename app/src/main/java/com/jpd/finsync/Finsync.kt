package com.jpd.finsync

import android.app.Application

class Finsync : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize global app state here if needed
    }
}
